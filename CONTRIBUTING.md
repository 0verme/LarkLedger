# 参与贡献

感谢你帮助改进 LarkLedger。请先通过 Issue 描述较大的功能或行为变更；小型修复可以直接提交 Pull Request。

## 开发流程

1. Fork 仓库并从 `main` 创建分支。
2. 使用 Python 3.11+ 创建虚拟环境，运行 `pip install -e ".[dev]"`。
3. 为行为变更补充测试。
4. 提交前运行 `ruff check .`、`mypy src` 和 `pytest --cov`。
5. PR 中说明问题、实现方式、验证结果，以及配置或迁移影响。

请勿提交 `.env`、真实凭据、用户账本、消息内容或可识别个人身份的数据。测试夹具应完全使用虚构信息。

## 设计原则

- PostgreSQL 是账本的唯一事实来源。
- AI 只能输出最小化、严格校验的业务意图，不能接触数据库连接或执行 SQL。
- 所有读写必须按飞书用户标识隔离。
- 涉及钱、时间与删除的行为必须有自动化测试。
- 默认保持自托管，避免引入强制的托管服务依赖。

参与本项目即表示你同意按项目的 Apache-2.0 许可证贡献代码。
