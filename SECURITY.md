# 安全策略

## 报告漏洞

请不要公开提交包含利用细节、真实凭据或个人财务数据的 Issue。请通过 GitHub 仓库的 Private vulnerability reporting 私下报告，并说明受影响版本、复现步骤、影响范围和建议修复方式。

维护者确认后会评估影响、准备修复并协调披露。当前项目处于早期阶段，仅最新版本接收安全更新。

## 部署建议

- 必须更换示例中的 PostgreSQL 密码，且不要将数据库端口暴露到公网。
- 使用 HTTPS 反向代理，并在飞书后台配置 Encrypt Key。
- AI 和飞书密钥仅通过环境变量或密钥管理服务注入；不要写入镜像或 Git。
- 仅授予飞书机器人运行所需的最小权限。
- 限制日志内容，避免记录消息正文、图片、语音、访问令牌和数据库连接串。
- 定期备份 PostgreSQL，并验证恢复流程。

AI 服务会收到用户提交的待理解内容。部署者应根据所选模型服务的隐私政策、数据驻留要求和组织合规规则进行配置。
